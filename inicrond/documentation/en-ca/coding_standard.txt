The language kjeywords in lowercase
Use ===
Use $array[] = $var instead of $array []= $var
concatenate string and avoid using dynamic variable interpolation
	use 'hello '.$you instead of "hello $you"
use 'array_key' instead of "array_key"
